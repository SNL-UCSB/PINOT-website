name: Jekyll build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: [self-hosted, pinot-website]

    steps:
    - name: Checkout current repo to the default location
      uses: actions/checkout@v3
      with:
        path: /srv/PINOT-website
